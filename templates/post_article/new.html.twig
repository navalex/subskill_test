{% extends 'base.html.twig' %}

{% block title %}{{ 'post.crud.new.title' | trans ~ parent() }}{% endblock %}

{% block body %}
    <div class="container mt-4">
        <div class="card">
            <div class="card-header">{{ 'post.crud.new.title' | trans }}</div>

            <div class="card-body">
                {{ include('post_article/_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
