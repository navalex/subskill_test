{% extends 'base.html.twig' %}

{% block title %}{{ 'post.crud.edit.title' | trans({'{title}': post_article.title}) ~ parent() }}{% endblock %}

{% block body %}
    <div class="container mt-4">
        <div class="card">
            <div class="card-header">{{ 'post.crud.edit.title' | trans({'{title}': post_article.title}) }}</div>

            <div class="card-body">
                {{ include('post_article/_form.html.twig') }}
            </div>

            <div class="card-footer">
                {{ include('post_article/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
